name: tests
on:
  push:
    branches:
      - main
  pull_request: null

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
    - name: checkout
      uses: actions/checkout@v2

    - name: build docker image
      run: |
        docker build .
